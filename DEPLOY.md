# 部署到腾讯云 EdgeOne Pages

「今天我做主」是纯静态应用（无后端、无构建、无依赖），部署 = 把 `dist/` 的文件传到云上。
本仓库的部署包已经打好了：`release/today-i-control-pwa.zip`（14 个文件，约 6 MB）。

---

## 为什么用 EdgeOne Pages

| | |
|---|---|
| 费用 | 免费套餐，不限量 CDN 流量与请求 |
| 域名 | 平台分配免费域名（`*.edgeone.app`），**不需要备案** |
| 上传 | 控制台拖拽 ZIP，约 20 秒上线 |
| 卸载/迁移 | 无锁定，换平台只需重传同一份 ZIP |

> 如果你之后办了 **ICP 备案的域名**，可以再建一个项目、加速区域选「中国大陆可用区」，国内访问会明显更快。
> 目前没有备案域名，所以选「全球可用区（不含中国大陆）」。

---

## 一、创建项目并上传

1. 浏览器打开 <https://console.cloud.tencent.com/edgeone/pages>，用微信或 QQ 登录腾讯云账号（首次进入点「立即开通」，免费）。
2. 点 **创建项目** → 选 **直接上传**。
   > ⚠️ 「直接上传」和「导入 Git 仓库」二选一后**不能互相切换**。这个项目没有 Git 构建流程，选直接上传即可。
3. 填写：
   - **项目名称**：`today-i-control`（要求 5–50 位，只能用**小写字母、数字、中划线**，不能以中划线开头/结尾/连续）
   - **加速区域**：选 **全球可用区（不含中国大陆）** ← 这样免备案
4. 把 `release/today-i-control-pwa.zip` 直接拖进上传区域。
   - 只接受**单个文件夹 / 单个 ZIP / 单个 HTML**，不要选多个零散文件
   - 本仓库的 ZIP 已保证 `index.html` 在**最外层**（多套一层目录会导致首页 404）
5. 点 **开始部署**，等 20 秒左右出现预览链接，例如 `https://today-i-control.edgeone.app`。

**上线后自检**：打开链接，四个页面（今天／计划／成长／奖励）都能用，右上角设置齿轮能进家长面板（PIN `1234`），完成一项挑战会加星星。

---

## 二、装到手机桌面（PWA）

应用已内置 `manifest.json` + Service Worker，可以像 App 一样装到桌面，**装好后没网也能打开**。

**Android（Chrome / Edge）**
1. 用手机浏览器打开你的 `*.edgeone.app` 链接
2. 点右上角「⋮」→ **添加到主屏幕** / **安装应用**
3. 桌面上会出现星星图标，点开是全屏、没有地址栏

**iPhone（Safari）**
1. 用 **Safari** 打开链接（必须 Safari，微信内置浏览器不行）
2. 点底部 **分享** 按钮 → **添加到主屏幕**

> 装了桌面的图标之后，以后要卸载/重装，数据会跟着浏览器存储走，见下面的「注意事项」。

---

## 三、以后怎么更新

改完 `dist/` 里的文件后：

```bash
node scripts/verify.js            # 必须全绿（项目回归门禁）
python scripts/pack-web.py        # 重新生成 release/today-i-control-pwa.zip
```

然后在 EdgeOne Pages 控制台 → 你的项目 → **新建部署** → 把新 ZIP 拖进去 → 部署环境选 **生产环境**。

**如果改了 HTML / CSS / JS**，记得把 `dist/sw.js` 顶部的 `VERSION` 加一：

```js
const VERSION = 'growth-v2';   // 原来是 growth-v1
```

否则用户浏览器里的旧缓存不会更新（Service Worker 靠版本号判断是否重建缓存）。

---

## 四、注意事项

**1. 数据存在浏览器里，不在云上。**
应用的成长记录保存在浏览器的本地存储中，并**按域名隔离**。这意味着：

- 上线后 `*.edgeone.app` 上的数据，和你在本地 `localhost:8080`、以及桌面版 `%USERPROFILE%\.growth` 的数据**互不相通**
- 换手机、换浏览器、清理浏览器数据，记录**不会自动同步，也不会自动备份**
- 域名一旦更换（比如以后绑了自定义域名），数据**不会跟着迁移**

家长面板里已有「备份与恢复」功能，建议定期导出 JSON 留一份。若希望手机和电脑共享同一份记录，需要另做云端同步（可评估 CloudBase 或其它后端方案）。

**2. 插画已转为 WebP。**
`assets/` 里 3 张原始 PNG 共 5.6 MB，已转成 `dist/` 下的 WebP（合计约 0.44 MB，**减少 92%**），首屏总重约 570 KB。以后换图：把新图放进 `assets/` 后执行 `python scripts/optimize-images.py`。注意 `growth-art` 是 CSS 雪碧图，脚本已固定按原尺寸转换——改它的尺寸会让任务卡图标全部错位。

**3. 家长 PIN 不是安全机制。**
PIN `1234` 只用于防误触，无法阻止孩子绕过，请勿把它当成真正的访问控制。

**4. 不用的项目及时删除。**
免费套餐有配额限制，页面上会显示用量概览。
