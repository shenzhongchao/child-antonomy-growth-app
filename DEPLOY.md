# 部署到腾讯云 CloudBase（静态网站托管）

「今天我做主」是纯静态应用（无后端、无构建、无依赖），部署 = 把 `dist/` 的文件传到云上。
本仓库的部署包已经打好了：`release/today-i-control-pwa.zip`（17 个文件，约 1.0 MB）。

---

## 先认清一件事：这条路必须备案，而且不快

这是动手前最该知道的，别做完才发现。

**1. 平台送的默认域名（`xxx.tcloudbaseapp.com`）不能给孩子长期用。**
官方原话是「默认域名仅建议用于开发测试，**严禁用于正式生产环境或分发给大规模用户**」：

- 浏览器直接打开会先跳一个 **「访问提示中间页」**，访客必须读完风险提示、点「确定访问」才能进到页面
- 有**访问频率限制**，加载会慢
- 平台实时监控，**流量异常可能直接封禁访问**

**2. 自定义域名强制要求 ICP 备案。** 没备案的域名在 CloudBase 里绑不上，绑上了也会被拦（403）。

**3. 备案要用 CloudBase 环境当备案资源，得同时满足三个条件：**

| 条件 | 要求 |
|---|---|
| 套餐版本 | **个人版及以上**（免费体验环境不支持备案） |
| 剩余有效期 | **≥ 6 个月** |
| 固定 IP | 该环境已开启 **「云托管固定 IP」** |

**4. 时间成本：腾讯云初审 1–2 个工作日，管局审核 20 个工作日内**（官方口径，多数省份实际 1–2 周）。

**5. 域名要先实名、实名后还要再等 3 个自然日**才能提交备案（详见下一节）。

**所以完整的上线节奏是这样的：**

```
第 1 天     买域名 → 提交实名认证（通常几小时到 1~3 个工作日）
+3 个自然日 实名信息入库工信部（这期间可以先部署 IP 包、可以先憋着）
第 N 天     开通环境 → 上传 ZIP → 默认域名能打开（有中间页，仅自测）
第 N 天     提交 ICP 备案 → 腾讯云初审 1~2 工作日 → 短信核验（须 24h 内）
第 N~N+3 周 管局审核
最后        备案通过 → 申请 SSL → 绑定域名 → 加 CNAME → 正式上线
```

> 如果你已经按 [CLOUD.md](CLOUD.md) 买了个人版环境做云端同步，那静态托管就跑在**同一个环境**里，
> 不用再单独买托管服务，配额也是共用这一份（个人版含静态托管容量 1 GB、存储流量 10 GB/月，
> 我们只占 1.5 MB，完全够）。

---

## 为什么选它（对比 EdgeOne Pages）

| | CloudBase 静态托管 | EdgeOne Pages（免备案路线） |
|---|---|---|
| 备案 | **必须备案**（1–20 工作日） | 选「全球可用区（不含中国大陆）」时免备案 |
| 国内速度 | 腾讯 CDN，国内秒开 | 境外节点，国内访问不稳定 |
| 费用 | 个人版 ¥19.9/月（已为同步买过则不加钱） | 免费套餐 |
| 上线周期 | 等备案，通常 2–3 周 | 当天 |
| 与云端同步 | **同一环境、同一控制台**，安全域名一处配好 | 两端分离，需额外配置 |
| HTTPS | 需自己申请免费 DV 证书并绑定 | 绑定域名后自动免费签发 |

**结论**：追求国内稳定、长期给孩子用 → CloudBase；想今天就能打开 → EdgeOne。
两者不冲突，备案等待期完全可以先用 EdgeOne 顶着（见文末附录 B）。

---

## 〇、挑域名并完成实名认证

这一步看着简单，但踩坑代价不小——**域名注册成功后不能退款**，买错了只能认栽。

### 三个硬性规则（腾讯云备案官方文档）

1. **后缀必须在工信部批复名单里。** 不在名单上的后缀，系统直接提示「暂不支持备案」。
   能备案的常见后缀：`.cn`、`.com.cn`、`.com`、`.net`、`.org`、`.net.cn`、`.org.cn`、`.top`、`.xyz`、`.shop`、`.vip`、`.club`、`.site`、`.online` 等。
   **不能备案的**：`.io`、`.ai`、`.dev`、`.app`、`.tv` 等。

2. **域名实名信息必须与备案主体完全一致。** 个人备案比对三项：姓名、证件类型、证件号码。
   工信部 2018 年起的规矩是「个人性质备案的域名注册者应为本人」——也就是说，
   你用谁的身份证做的实名，就必须用同一个人的身份去备案，不能拿家人的证件注册域名再自己备案。

3. **实名认证通过后要再等 3 个自然日才能提交备案**（腾讯云注册的域名；在外面注册的域名要等 3 个工作日）。
   这是给实名信息同步进工信部库的时间，急着提交会被管局以「查不到最新实名信息」驳回。

### `.com.cn` 个人能不能注册？有风险

按 CNNIC 的官方定义，`.CN` 下的类别域名 `COM` 是「适用于工、商、金融等**企业**」的。
腾讯云自己的文档把 `.com.cn` 和 `.cn` 并列在「国内域名」里、同时说明了个人和企业两种实名方式，
看上去是在卖；但包括新网在内的部分 CNNIC 认证注册商明确答复「`.com.cn` 不接受个人名义申请」。

**各家注册商执行口径不一致，而域名买了不退。** 所以个人使用的推荐顺序是：

| 推荐度 | 后缀 | 说明 |
|---|---|---|
| 最稳 | `babygrowth.cn` | 个人注册毫无争议，CNNIC 直管，备案最顺，首年通常几十块 |
| 也稳 | `babygrowth.com` | 无注册人性质限制，隐私可隐藏，首年约 70–90 元 |
| 有风险 | `babygrowth.com.cn` | 可能被要求必须是企业/组织持有者，个人实名有被驳回的可能 |

> 如果你确实想要 `.com.cn`：先在腾讯云用**个人信息模板**把域名加进购物车，
> 看系统有没有拦；支付前问一次客服确认「个人能否完成实名」。确认了再付款。

### 命名规则

只能用英文字母（`a–z`，大小写等价）、数字（`0–9`）和连字符 `-`；
不能以 `-` 开头或结尾；`.com.cn` / `.cn` 下三级标签长度通常 3–63 字符。
`babygrowth` 是纯小写英文、10 个字符，**格式完全没问题**。

### 用子域名更省事

**网站备案只需备案二级域名**，备案后三级、四级域名可以直接用。
也就是说备案完 `babygrowth.cn` 之后，`today.babygrowth.cn` 不用再单独备案。

建议**给应用用子域名**，例如 `today.babygrowth.cn`：

- 加 CNAME 记录更干净（根域名的 `@` 记录容易和邮箱解析等其他记录打架）
- 以后同一个域名下还能挂别的站点

> 注意申请 SSL 证书时要申请**子域名这张**（`today.babygrowth.cn`），别只申请主域名。

---

## 一、开通静态网站托管并上传

1. 打开 <https://tcb.cloud.tencent.com/> ，进入你**已有的那个环境**（就是 `dist/config.js` 里填的 `envId`）。
   还没建的话先按 [CLOUD.md](CLOUD.md) 建一个个人版环境。
2. 左侧菜单 → **静态网站托管** → 首次进入点 **开启**（页面会提示开通）。
3. 点 **本地项目部署**（= 上传代码包/文件夹），把 `release/today-i-control-pwa.zip` 拖进去。
4. 填这几个字段：

   | 字段 | 填什么 |
   |---|---|
   | 应用名称 | `today-i-control` |
   | 构建命令 | **留空**（纯静态，没有构建步骤） |
   | 构建产物目录 | `.` （表示包的根目录） |
   | 部署路径 | `/` |

5. 点 **开始部署**，约 1–2 分钟完成，状态变「成功」。
6. 点 **访问应用**，浏览器会打开 `https://xxx.tcloudbaseapp.com`。
   第一次会看到「访问提示中间页」，点 **确定访问** 进到应用 —— 这说明部署成功了。

> ⚠️ 本仓库的 ZIP 已保证 `index.html` 在**最外层**。如果你自己打包，多套一层目录会导致首页 404。
> 用 `python scripts/pack-web.py` 打包会自动做这个校验。

**上线前先跑一遍回归**（改过 `dist/` 的话）：

```bash
node scripts/verify.js            # 必须全绿
python scripts/pack-web.py        # 重新生成 release/today-i-control-pwa.zip
```

---

## 二、申请 SSL 证书

自定义域名开 HTTPS 必须自己准备证书，CloudBase 不会自动签发。

1. 打开 <https://console.cloud.tencent.com/ssl> → **申请免费证书**（免费版 DV SSL，有效期 1 年）。
2. 通用名称填你准备绑定的域名，例如 `today.你的域名.com`。
3. 按提示完成**域名身份验证**（通常是加一条 DNS TXT 记录），等签发，一般十几分钟到几小时。
4. 状态变成「已签发」就可以在下一步选到它了。

> 免费证书只有 1 年有效期，到期前要去重新申请并回来换绑，手机上会报证书错误。
> 建议设个日历提醒。

---

## 三、办 ICP 备案

### 3.1 确认环境满足三个条件

控制台 → 你的环境 → **环境设置 → 备案管理**。页面会直接列出还差哪条：

- 套餐是**个人版及以上**（免费体验环境不支持）
- 剩余有效期 **≥ 6 个月**（不够就先续费）
- 已开启 **云托管固定 IP**（在「云托管」相关设置里打开）

### 3.2 提交

在「备案管理」页点 **去备案** → 跳转腾讯云备案控制台 → 拉取你的 CloudBase 环境作为备案云资源 →
按指引填主体信息、网站信息、上传材料、人脸核验 → 提交初审 → 管局审核。

**通常需要 1–20 个工作日。**

### 3.3 两个常见坑

- **域名在别家已经备过案**：不能直接绑，要先办「备案转入（新增接入）」把备案接入腾讯云。
- **域名要先完成实名认证**：注册域名后必须实名，否则会被注册局暂停解析（Serverhold）。

---

## 四、绑定自定义域名

备案通过后：

1. 控制台 → 环境 → **静态网站托管** → 点进应用名称 → **自定义域名** → **添加自定义域名**。
2. 填写：

   | 配置项 | 填什么 |
   |---|---|
   | 域名 | 要对外访问的完整域名，例如 `today.你的域名.com` |
   | 绑定模式 | **应用模式**（整站映射到该域名，根路径即进站点） |
   | SSL 证书 | 选第二步申请好的那张 |
   | CDN 类型 | **云开发 CDN**（静态站点自动加速；选定后不建议再改） |

3. 提交后平台需要 **3–5 分钟**完成域名关联，然后会给一条 CNAME 记录值（形如 `xxx.cdn.dnsv1.com`），**复制下来**。
4. 去你的 DNS 服务商（腾讯云 DNSPod / 阿里云等）添加记录：

   | 主机记录 | 记录类型 | 记录值 |
   |---|---|---|
   | `today` | CNAME | 控制台给的那串 CNAME |

   > ⚠️ **同一主机记录下 CNAME 与 A、TXT 等记录类型会冲突**。保存时提示「记录有冲突」，
   > 就先把该主机记录下已有的其它记录删掉，再加 CNAME。

5. DNS 通常 **5–30 分钟**生效。自查：

   ```bash
   nslookup today.你的域名.com
   ```

   返回结果里出现 CloudBase 给的 CNAME 域名，就说明解析生效了。
6. 回控制台刷新，域名状态变成 **「正常 / 已生效」**，浏览器打开 `https://today.你的域名.com` 能看到应用，即完成。

> **换域名后务必回到 [CLOUD.md](CLOUD.md) 的「安全配置」，把新域名加进 Web 安全域名**，
> 否则家长登录和云端同步会被直接拒绝（表现为点登录没反应）。

---

## 五、装到手机桌面（PWA）

应用已内置 `manifest.json` + Service Worker，可以像 App 一样装到桌面，**装好后没网也能打开**。

**Android（Chrome / Edge）**
1. 用手机浏览器打开 `https://today.你的域名.com`（必须是绑好、且 HTTPS 已生效的那个）
2. 点右上角「⋮」→ **添加到主屏幕** / **安装应用**
3. 桌面上会出现星星图标，点开是全屏、没有地址栏

**iPhone（Safari）**
1. 用 **Safari** 打开（必须 Safari，微信内置浏览器不行）
2. 点底部 **分享** → **添加到主屏幕**

**上线后自检**：四个页面（今天／计划／成长／奖励）都能用；右上角齿轮能进家长面板（PIN `1234`）；
完成一项挑战会加星星且不能重复加；家长面板显示唯一孩子设置与「记录保护」，绑定测试手机号后等待后台自动保护成功。

---

## 六、以后怎么更新

改完 `dist/` 里的文件后：

```bash
node scripts/verify.js            # 必须全绿（项目回归门禁）
python scripts/pack-web.py        # 重新生成 release/today-i-control-pwa.zip
```

然后 CloudBase 控制台 → 静态网站托管 → 你的应用 → 重新上传新的 ZIP。

**如果改了 HTML / CSS / JS，记得把 `dist/sw.js` 顶部的 `VERSION` 加一**：

```js
const VERSION = 'growth-v3';   // 原来是 growth-v2
```

否则用户浏览器里的旧缓存不会更新（Service Worker 靠版本号判断是否重建缓存）。

另外还给 `index.html` 里的资源加了 `?v=10` 之类的版本号，改文件时也可以顺手加一，双保险。

**CDN 缓存**：控制台「设置 → 节点缓存配置」可以调缓存时间，改完 1–3 分钟生效。
如果发现发了新版但手机上还是旧的，去那里点一次 **刷新缓存**。
建议把 `index.html` 的缓存时间调短（比如 1 小时），图片/CSS/JS 可以放长（7–30 天）。

---

## 七、注意事项

**1. 数据默认只存在浏览器里，且按域名隔离。**
未开云端同步时，成长记录保存在浏览器的本地存储中，并**按域名隔离**。这意味着：

- 换域名后数据不会自动跟过去（隔离所致），要靠云端同步或家长面板的备份来搬
- 未开云端同步时，换手机、换浏览器、清理浏览器数据，记录**不会自动同步，也不会自动备份**

> **换域名时靠云端同步搬数据最省事**：先在旧域名上登录家长账号同步一次，
> 再在新域名上登录同一个手机号，记录就拉回来了。这是选 CloudBase 的一个实际好处——
> 托管和同步在同一个环境里，配合得最好。

**2. 插画已转为 WebP。**
`assets/` 里 3 张原始 PNG 共 5.6 MB，已转成 `dist/` 下的 WebP（合计约 0.44 MB，**减少 92%**）。
首屏约 570 KB（未开云端同步时）；`cloudbase.esm.js`（582 KB）只在家长登录时按需加载，不进首屏。
以后换图：把新图放进 `assets/` 后执行 `python scripts/optimize-images.py`。
注意 `growth-art` 是 CSS 雪碧图，脚本已固定按原尺寸转换——改它的尺寸会让任务卡图标全部错位。

**3. 家长 PIN 不是安全机制。**
PIN `1234` 只用于防误触，无法阻止孩子绕过，请勿把它当成真正的访问控制。

**4. SSL 证书会过期。**
免费 DV 证书 1 年有效，到期前记得重新申请并回来换绑，否则手机上会跳证书警告。

**5. 静态托管会产生存储费用。**
量很小（1.5 MB，个人版 1 GB 配额内免费），但如果哪天不用了，去控制台**注销静态网站**，
否则会持续占用和计费。

---

## 附录 A：出问题先看这里

| 现象 | 原因 / 修法 |
|---|---|
| 打开默认域名跳「访问提示中间页」 | 正常现象，点「确定访问」即可；长期用请绑自定义域名 |
| 自定义域名访问 403 | 域名没过 ICP 备案，或备案已过期 |
| 报 `ERR_CERT_COMMON_NAME_INVALID` | 证书绑的域名不对，或证书过期 |
| 首页能开，CSS/JS 全 404 | ZIP 里 `index.html` 没在最外层，或部署路径填错（应为 `/`） |
| 发了新版但手机还是旧的 | `dist/sw.js` 的 `VERSION` 没加一，或 CDN 缓存没刷新 |
| 点家长登录没反应 | CloudBase「安全配置」里没加当前域名 |
| DNS 加了 CNAME 仍不生效 | 同一主机记录下有 A/TXT 记录冲突，或本地 DNS 缓存（等 5–30 分钟） |
| 添加到主屏幕没反应 | 必须是 HTTPS + 已绑好的自定义域名；iOS 必须用 Safari |

---

## 附录 B：备案等待期怎么给孩子用

备案要等 1–20 个工作日，这段时间的过渡方案，按推荐顺序：

**1. EdgeOne Pages 临时站点（免备案，但必须先绑自定义域名）**
在 <https://console.cloud.tencent.com/edgeone/pages> 建项目，加速区域选
**「全球可用区（不含中国大陆）」**（免备案的关键），上传同一个 ZIP。
然后**必须去「项目设置 → 域名管理」绑一个自定义域名**（如临时用 `temp.你的域名.cn`，加 CNAME 即可），
否则在国内打不开。

> ⚠️ 这个区域下，平台给的项目域名／部署域名**在中国大陆网络一律返回 401**（官方规则），
> 只有绑了自定义域名才会给你中国大陆可直接访问的通道（此区域绑域名免备案）。
> 也就是说：上传完不绑域名 = 你自己在国内根本打不开，这不是配置出错。
>
> 等 CloudBase 备案通过后，把 CNAME 改指 CloudBase，或者直接用另一个子域名，两者别混用。

缺点是页面资源走海外 CDN，国内加载会稍慢。

> 不推荐在备案等待期把默认域名当正式入口给孩子用：中间页每次都要点确认，
> 且平台明确说可能因风控被封禁。
